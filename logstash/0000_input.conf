input {
  kafka {
    bootstrap_servers => ""
    topics => [
      "siem-falco",
      "siem-k8s-audit",
      "siem-k8s",
      "siem-tetragon"
    ]
    codec => "json"
    group_id => "logstash"
    poll_timeout_ms => 100
    auto_offset_reset => "latest"
    tags => [
      "${POD_NAME}"
    ]
  }
}
